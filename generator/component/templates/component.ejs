<template>
  <div class="<%= componentName %>">
    <%= componentNamePascalCase %>
  </div>
</template>

<script>
import { defineComponent } from 'vue';<% if (vuex) { %>
import { <%= vuex %> } from 'vuex';<% } %>

export default defineComponent({
  name: '<%= componentNamePascalCase %>',

  /**
   * 属性
   */
  props: {},

  /**
   * 数据
   */
  data() {
    return {};
  },

  /**
   * 计算属性
   */
  computed: {<% if (mapGetters) { %>
    ...mapGetters({}),<% } %>
  },

  /**
   * 已创建
   */
  created() {
  // 
  },

  /**
   * 组件方法
   */
  methods: {<% if (mapMutations) { %>
    ...mapMutations({}),<% } %><% if (mapActions) { %>
    ...mapActions({}),<% } %>
  },

  /**
   * 使用组件
   */
  components: {
  },
});
</script>

<style scoped>
@import './styles/<%= componentName %>.css';
</style>
